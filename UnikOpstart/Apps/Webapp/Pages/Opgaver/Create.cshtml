@page
@model Webapp.Pages.Opgaver.CreateModel
@{
}
<h2>Opret Opgave</h2>
<div class="row">
    <div class="col-md-6">
        <form method="post">
            <div class="form-group">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                
                <label asp-for="CreateViewModel.Title"></label>
                <input asp-for="CreateViewModel.Title" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="CreateViewModel.Process_Kategori"></label>
                <select asp-for="CreateViewModel.Process_Kategori" class="form-select" aria-required="true">
                    <option selected>...</option>
                    <option value="Teknik">Teknik</option>
                    <option value="Konvertering">Konvertering</option>
                    <option value="Konsultation">Konsultation</option>
                </select>

                <span asp-validation-for="CreateViewModel.Process_Kategori" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CreateViewModel.KompetenceId">Kompetence Behov</label>
                <select asp-for="CreateViewModel.KompetenceId" class="form-control" id="asd" aria-required="true">
                    <option selected>...</option>
                    @foreach (var kompetence in Model.KompetenceIndexViewModels)
                    {
                        <option value="@kompetence.Id">@kompetence.Egenskab</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="CreateViewModel.TimeEstimat">Tidsestimat (i dage)</label>
                <input asp-for="CreateViewModel.TimeEstimat" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="CreateViewModel.Kommentar"></label>
                <input asp-for="CreateViewModel.Kommentar" class="form-control" />
            </div>

            <button class="btn btn-success" type="submit" class="btn btn-default" asp-route-id="@Model._projektId">Opret Opgave</button>
        </form>
    </div>
</div>